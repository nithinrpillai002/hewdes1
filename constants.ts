export const MOCK_DELAY_MS = 0;

export const DEFAULT_SETTINGS = {
  appId: '',
  webhookUrl: window.location.origin + '/webhook/instagram',
  webhookVerifyToken: 'instagram-crm-verify-token',
  pageAccessToken: '',
  kieApiKey: ''
};
